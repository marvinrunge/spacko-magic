on:
  push:
    branches:
    - master
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: npm install & build
      run: |
        node -v
        ls
        npm i
        npm run build
